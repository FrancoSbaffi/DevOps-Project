events {}

http {
    # Intento de upstream para app3 (incompleta)
    upstream app3_incompleta {
        # La idea era apuntar a un contenedor app3
        # pero todavia no se definió un alias ni se integró correctamente.
        # Este bloque queda así, a medio hacer.
        # server app3:80; # Comentado definir si se implementa o no luego.
    }

    server {
        listen 80;

        # aca se podria redirigir /app3/ a app3_incompleta
        # pero no se completó el upstream, así que este bloque es simbólico.

        # location /app3/ {
        #    proxy_pass http://app3_incompleta/;
        #    # Falta la configuración de headers, si esto sigue, terminarlo.
        # }

        # Otros intentos de configuración podrían ir acá,
        # comentados o a medio definir.
    }
}
