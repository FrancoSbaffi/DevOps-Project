FROM node:14-alpine

WORKDIR /usr/src/app

# Copiamos package.json primero para aprovechar cache en caso de cambios
COPY package.json ./
RUN npm install

COPY index.js ./

# Los datos se montarán via bind mount en runtime, no se copian acá
EXPOSE 3000
CMD ["node", "index.js"]
